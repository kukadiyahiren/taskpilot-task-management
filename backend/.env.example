# --- MySQL (used to build DATABASE_URL automatically) ---
MYSQL_USER=teamtask
MYSQL_PASSWORD=password
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DB=team-task-board

# Optional: set this to override everything above (e.g. SQLite for local file only)
# DATABASE_URL=sqlite:///./taskpilot.db

# --- JWT (for future / current auth endpoints) ---
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

CORS_ORIGINS=http://localhost:5142,http://127.0.0.1:5142

# When true, POST /auth/forgot-password returns reset_token in JSON (no SMTP). Set false in production.
PASSWORD_RESET_RETURN_TOKEN=true
