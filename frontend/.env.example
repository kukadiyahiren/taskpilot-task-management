# Where FastAPI runs (Vite dev server proxies /api → this URL)
# Default in vite.config.js is 8010. Override if needed, e.g. port 8000:
# API_PROXY_TARGET=http://127.0.0.1:8000
API_PROXY_TARGET=http://127.0.0.1:8010
